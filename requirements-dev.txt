# 测试与开发依赖（CI 与本地测试用；运行依赖见 requirements.txt）
-r requirements.txt
httpx>=0.27          # fastapi TestClient 依赖
python-xlib>=0.33    # Linux 测试环境（Xvfb 下的 pyautogui）
ruff>=0.9            # CI 的 F/E9 静态检查（workflow 运行 python -m ruff check）
