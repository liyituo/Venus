__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
venv/
*.log
chat_config.json
*.bak
chat_config.json.bak

# ZCode 工具内部文件
.zcode/

# 本地会话数据（含聊天记录，严禁入库）
.venus/
.pcagent/

# Telegram bot 配置（含 bot token，严禁入库）
telegram_config.json

# MCP server 配置（含第三方 token，严禁入库）
mcp_config.json

# 本地工具二进制（不入库）
tools/

# 量化子项目作为隔离子目录纳入主仓库；其缓存、数据库、日志和构建产物
# 由 quant-agent-lab/.gitignore 单独排除。

# 本地 AI 额度面板：源码入库；凭据与窗口位置见 local-quota-widget/.gitignore
